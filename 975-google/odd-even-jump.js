/**
 * @param {number[]} arr
 * @return {number}
 */
var oddEvenJumps = function(arr) {

    const goodIndexes = []

    for (let i = 0; i < arr.length; i++) {
        let jump = 0;
        const element = arr[i];
        
        for(let j = i; j < arr.length; j++) {

        }


    }
    
};

result = oddEvenJumps([10,13,12,14,15])

console.log('final result', result)